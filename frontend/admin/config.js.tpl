window.APP_CONFIG = {
  cognitoDomain: "${cognito_domain}",
  clientId: "${client_id}",
  apiBase: "${api_base}",
  redirectUri: "${redirect_uri}",
  logoutUri: "${logout_uri}",
  siteOrigin: "${site_origin}",
};
